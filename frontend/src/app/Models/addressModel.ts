import * as internal from "stream";

export interface IAddressModel {
    firstName: string;
    lastName: string;
    city: string;
    street: string;
    streetNumber: number;
    details: string;
    zipCode: string;
}